namespace ScreenTimeTracker.Models
{
    /// <summary>
    /// Defines the view mode for the chart (hourly or daily)
    /// </summary>
    public enum ChartViewMode
    {
        Hourly,
        Daily
    }
} 